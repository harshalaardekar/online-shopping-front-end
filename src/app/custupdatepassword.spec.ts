import { Custupdatepassword } from './custupdatepassword';

describe('Custupdatepassword', () => {
  it('should create an instance', () => {
    expect(new Custupdatepassword()).toBeTruthy();
  });
});
